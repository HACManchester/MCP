server {
    listen 80;

    location / {

            proxy_pass http://127.0.0.1:5000$request_uri;
            proxy_set_header Host $host;
    }

    location /static {
            alias  /srv/mcp/static;
    }
}
